syntax = "proto3";

enum ClientMsgId{
	C2S_HEART_BEAT = 0;
	C2S_GROUP_MESSAGE_INFO = 1;
	C2S_GROUP_MESSAGE_FILTER = 2;
}

message BaseMessage {
	// 消息类型名称，必需为英文
	int32 type = 1;

	// 实际消息数据
	bytes message_data = 2;
}